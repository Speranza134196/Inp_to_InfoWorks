<!DOCTYPE html>
<html>
<head>
  <title>INP to InfoWorks Exporter</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <!-- Logo -->
  <div class="text-center mb-4">
    <img src="/logo.png" alt="Logo" style="max-width: 200px;">
  </div>

  <h1 class="mb-4 text-center">INP to InfoWorks Exporter</h1>

  <% if @message %>
    <div class="alert <%= @message.include?("✅") ? "alert-success" : "alert-danger" %>">
      <%= @message %>
    </div>
  <% end %>

  <form action="/upload" method="post" enctype="multipart/form-data" class="mb-4">
    <div class="mb-3">
      <label for="inp_file" class="form-label">Choose INP file:</label>
      <input class="form-control" type="file" name="inp_file" accept=".inp" required>
    </div>
    <button class="btn btn-primary">Export</button>
  </form>

  <% if @download_links %>
    <div class="card p-3">
      <h5>📥 Download your files:</h5>
      <ul>
        <li><a href="<%= @download_links[:nodes] %>" download>📄 nodes.csv</a></li>
        <li><a href="<%= @download_links[:links] %>" download>📄 links.csv</a></li>
        <li><a href="<%= @download_links[:zip] %>" download>📦 Download ZIP</a></li>
      </ul>
    </div>
  <% end %>
</body>
</html>
